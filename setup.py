from setuptools import setup

setup(
    name = "basic_stats",
    version = "0.1",
    author = "Mansur Ali Jisan",
    packages=['basic_stats1'],
    install_requires=['numpy','matplotlib'],
)
